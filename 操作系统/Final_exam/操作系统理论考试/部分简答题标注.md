# 1.实现多道程序设计需要哪些必须的硬件设施？（CPU、内存、总线、外部存储器除外）p36
中断、特权指令、内存保护、定时器。
# 2.什么是系统调用？从操作系统角度看，它们在运行状态上与普通函数有什么区别？
第二章不知道在哪一页

系统调用：当用户程序调用操作系统所提供的函数时成为系统调用

区别：系统调用需要内核的参与，要在内核模式下执行，需要模式切换；普通函数在用户模式下执行，不需模式切换

# 关于RAID0-RAID5的问题
## 1、比较RAID0和RAID1的读写效率？
RAID0:高数据传送能力，高速IO请求率，读写速度都很快

RAID1:读是单个磁盘的两倍，写和一个磁盘相近？

## 2、如果磁盘系统是RAID0时，那么为了有利于多进程读写磁道，磁盘条带应该大写还是少些？为什么？

大一些，这样一个I/O请求的内容更有可能在同一磁盘中，能够实现多个磁盘的并行访问

## 3、在事务型应用(即存在多个并发的I/O,每次1/O的数据量不大)中,RAID0的条块尺寸是大些好还 是小些好?对于大数据传输呢?

【参考答案】事务型应用:大些好。大数据传输:小些好 看不懂：有人看懂了吗

## 4、RAID1和RAID5相比，哪个可靠性更好，哪个写磁盘更快？为什么？
RAID1可靠性更好，所有磁盘都有备份

RAID1更快，并行写入磁盘，接近单个磁盘的速度；RAID5要先读取旧校验信息，得出新校验信息，写入更新校验信息，再写入